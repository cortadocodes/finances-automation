version: '3.2'

services:

  app:
    image: finances-automation:latest
    restart: always
    depends_on:
      - db

  db:
    image: postgres:latest
    restart: always
    ports:
      - ${DB_PORT}:5432
    volumes:
      - "./data/database_cluster:/var/lib/postgresql/data"
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
